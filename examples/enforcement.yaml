version: 1
enabled: true
post_mutation_policy_feedback: true
completion_claim_only: true
max_stop_blocks_per_session: 3
# Eligibility is defined by scripts/action_guard.py for both hooks and state/CLI.
# The retry limit stops automatic continuation; it never grants completion.
